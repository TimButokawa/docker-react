import { render, screen } from '@testing-library/react';
import App from './App';

test('renders', () => {
  expect(render(<App />)).toBeTruthy();
});

test('renders', () => {
  expect(render(<App />)).toBeTruthy();
});
